repos:
  # Local hooks
  - repo: local
    hooks:
      - id: make-before-commit
        name: make before_commit
        entry: make before_commit
        language: system
        always_run: true
        pass_filenames: false
