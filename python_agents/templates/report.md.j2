# GCP Security Audit Report

**Generated:** {{ metadata.timestamp }}  
**Project:** {{ metadata.project_id }}  
**Organization:** {{ metadata.organization_id }}  
{% if metadata.collection_timestamp %}**Data Collected:** {{ metadata.collection_timestamp }}{% endif %}

---

## Executive Summary

The security audit for project **{{ metadata.project_id }}** identified **{{ findings|length }} total findings**:

{% set high_count = findings|selectattr('severity', 'equalto', 'HIGH')|list|length %}
{% set medium_count = findings|selectattr('severity', 'equalto', 'MEDIUM')|list|length %}
{% set low_count = findings|selectattr('severity', 'equalto', 'LOW')|list|length %}

- 🔴 **{{ high_count }} HIGH** severity issues requiring immediate attention
- 🟡 **{{ medium_count }} MEDIUM** severity issues to be addressed
- 🟢 **{{ low_count }} LOW** severity issues for consideration

{% if high_count > 0 %}
⚠️ **Action Required:** High severity issues were found that pose significant security risks. Please review and remediate these findings immediately.
{% endif %}

---

## Severity Distribution

| Severity | Count | Percentage |
|----------|-------|------------|
| HIGH     | {{ high_count }} | {{ "%.1f"|format((high_count / findings|length * 100) if findings else 0) }}% |
| MEDIUM   | {{ medium_count }} | {{ "%.1f"|format((medium_count / findings|length * 100) if findings else 0) }}% |
| LOW      | {{ low_count }} | {{ "%.1f"|format((low_count / findings|length * 100) if findings else 0) }}% |

---

## Detailed Findings

{% for severity in ['HIGH', 'MEDIUM', 'LOW'] %}
{% set severity_findings = findings|selectattr('severity', 'equalto', severity)|list %}
{% if severity_findings %}

### {{ severity }} Severity Issues

{% for finding in severity_findings %}
#### {{ loop.index }}. {{ finding.title }}

**Severity:** {{ finding.severity }}

**Description:**  
{{ finding.explanation }}

**Recommendation:**  
{{ finding.recommendation }}

---

{% endfor %}
{% endif %}
{% endfor %}

## Remediation Priority

Based on the findings, here's the recommended remediation priority:

1. **Immediate (HIGH severity):** Address all HIGH severity findings within 24-48 hours
2. **Short-term (MEDIUM severity):** Plan to address MEDIUM findings within 1-2 weeks
3. **Long-term (LOW severity):** Include LOW findings in your next security review cycle

---

## Report Metadata

- **Report Format:** Markdown (Obsidian-compatible)
- **Generated By:** Paddi - Multi-Agent Cloud Audit Tool
- **Version:** 1.0.0
- **Tags:** #security #gcp #audit #{{ metadata.project_id|replace('-', '_') }}

---

*This report was automatically generated. For questions or concerns, please contact your security team.*